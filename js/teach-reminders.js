/* Shows due dashboard reminders while the teacher is on Teach. */
(function(){"use strict";const KEY="patriotDashboardRemindersV1";function read(){try{return JSON.parse(localStorage.getItem(KEY))||[]}catch(e){return[]}}function save(v){localStorage.setItem(KEY,JSON.stringify(v))}function check(){const now=Date.now(),items=read();const due=items.filter(r=>r.teach&&!r.dismissed&&new Date(r.date+"T"+r.time).getTime()<=now);if(!due.length)return;let bar=document.getElementById("patriot-teach-reminder");if(!bar){bar=document.createElement("div");bar.id="patriot-teach-reminder";bar.style.cssText="position:fixed;z-index:9999;left:50%;top:12px;transform:translateX(-50%);width:min(720px,calc(100% - 24px));padding:12px 14px;color:#fff;background:#9f201a;border:2px solid #d3a84f;border-radius:11px;box-shadow:0 10px 30px rgba(0,0,0,.22);font:700 14px/1.35 Inter,Segoe UI,sans-serif;display:flex;align-items:center;justify-content:space-between;gap:12px";document.body.appendChild(bar)}const r=due[0];bar.innerHTML=`<span>⏰ Reminder: ${String(r.text).replace(/[&<>]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;"}[c]))}</span><button type="button" style="border:0;border-radius:7px;padding:7px 10px;font-weight:800;cursor:pointer">Dismiss</button>`;bar.querySelector("button").onclick=()=>{const all=read();const x=all.find(i=>i.id===r.id);if(x)x.dismissed=true;save(all);bar.remove();setTimeout(check,50)}}if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",check);else check();setInterval(check,15000)})();